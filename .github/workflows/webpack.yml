name: Build tenha fibra

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.13.0]

    steps:
    - uses: actions/checkout@v3

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}

    - name: Clear caches and remove node_modules
      run: npm cache clean --force && rm -rf node_modules

    - name: Install dependencies
      run: npm install --force

    - name: Build
      run: npm run build

    - name: Verificar mudanças no package.json
      id: check_changes
      run: |
        git diff --quiet HEAD..HEAD~1 -- package.json || echo "::set-output name=changes::true"
                
    - name: Add e commit dos arquivos de build
      if: steps.check_changes.outputs.changes == 'true'
      uses: EndBug/add-and-commit@v7
      with:
        message: "Atualizar arquivos de build [skip ci]"
        add: "dist"
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
